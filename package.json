{
  "name": "portfolio-platter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "diff": "lerna diff",
    "start-web": "cd packages/web && npm start",
    "start-server": "cd packages/server && npm start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "mongodb": "^6.1.0",
    "mongoose": "^7.5.3",
    "react-icons": "^5.0.1"
  }
}
